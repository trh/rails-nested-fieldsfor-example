%h1 Listing domains

%table
  %tr
    %th Domain name
    %th Nameserver first
    %th Nameserver second
    %th
    %th
    %th

  - @domains.each do |domain|
    %tr
      %td= domain.domain_name
      %td= domain.nameserver_first
      %td= domain.nameserver_second
      %td= link_to 'Show', domain
      %td= link_to 'Edit', edit_domain_path(domain)
      %td= link_to 'Destroy', domain, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Domain', new_domain_path
